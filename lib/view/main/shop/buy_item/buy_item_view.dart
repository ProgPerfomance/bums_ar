import 'package:flutter/material.dart';

class BuyItemView extends StatelessWidget {
  const BuyItemView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
